---
title: "AIと二人三脚で作ったリアルタイム対戦アプリを、学祭で130人に同時プレイさせるまで"
emoji: "🌍"
type: "tech"
topics: ["websocket", "nodejs", "react", "個人開発", "AI活用"]
published: false
---

## この記事について

学祭のステージ企画用に、来場者のスマホ130台が**同時にリアルタイム対戦する**Webアプリを個人開発し、本番運用まで持っていった記録です。

タイトルの通り、コードのかなりの部分はAI（生成AI）と一緒に書きました。なので本記事は「すごいものを一人で作った自慢」ではなく、

- **AIに任せる部分／自分で判断しなければ死ぬ部分をどう切り分けたか**
- **その結果、本番イベントで実際に何が起きたか**

という2軸で書きます。個人開発でAIを使い倒しているけど「本番で動かす」ところに不安がある人に、いちばん刺さると思います。

### 前提:魅儡祭と、私の立場

私は大阪国際工科専門職大学の**学生**で、**運営スタッフ70名ほど**の学祭「魅儡祭」の運営に関わっていました。魅儡祭は大きく**ブース企画**と**ステージ企画**に分かれていて、この記事のアプリは後者、**ステージ企画そのもの**です。ステージで行う2つの企画（後述のGEOモードとGOT TALENTモード）を、この1つのアプリで回しました。

そして正直に書くと、私が学祭で背負っていた役割はアプリ開発だけではありませんでした。

- **実行委員長（代表）** — 学祭全体の統括
- **ステージ企画担当** — 当日のステージ進行そのもの
- **照明・スクリーン統括** — 会場演出の技術面
- **技術・IT担当** — そしてこのアプリの開発

要するに、**学祭全体を回す人間が、当日ステージで使うアプリも一人で作っていた**という状況です。この「圧倒的に時間がない」前提が、この記事のAIの使い方すべての土台になっています。

### 作ったもの

上記ステージ企画のために作った、来場者のスマホが参加するリアルタイムアプリです。1つのアプリに2モードを載せました。

- **GEOモード**: 写真を見て「どこの場所か」を地図上のピンで当てる、GeoGuessr風の距離採点ゲーム
- **GOT TALENTモード**: ステージ上のパフォーマーに、客席のスマホから「Good（拍手）」投票してGood率を出す観客参加システム。有志パフォーマンス5組（ミュージカル・歌・ビートボックス・剣道・けん玉）が対象

管理者（司会席）の画面から出題・集計・ランキング表示をすべて制御し、参加者は**会場スクリーンに映したQRコード**を読んでURLを開くだけ。結果は同じスクリーンにリアルタイム反映します。

技術構成はシンプルです。

- フロント: React + Vite + Tailwind（Vercel配信）
- サーバー: Node.js + `ws`（WebSocket）を1プロセス、VM上でsystemd管理
- 状態は全部サーバーのメモリ（DBなし。2時間ちょっとのステージ企画中しか使わないため）

---

## なぜ「AIと作った」話をわざわざ書くのか

前述のとおり、私は実行委員長・ステージ進行・照明/スクリーン統括を兼任しながら、このアプリを一人で作っていました。学祭の準備と当日運営を回しながらの開発なので、率直に言って、アプリだけにじっくり時間を割ける状況ではありませんでした。

AIをフル活用したのは「楽をしたかったから」ではなく、**限られた時間で本番に間に合わせるための現実的な選択**です。運営で手が埋まっている人間が、それでもリアルタイム対戦アプリを本番投入まで持っていく。そのための道具としてAIを使い倒した、というのがこの記事の実態です。

そのうえで正直に言うと、この規模のリアルタイムサーバーを、私が完全に一人でゼロから書ける実力があったわけではありません。WebSocketのブロードキャスト設計も、systemdの設定も、AIに壁打ちしながら組み上げました。

ただ、**本番で130人を落とさずに捌けたかどうか**は、AIが書いたか自分が書いたかとは別の話です。落ちるときは落ちます。そして落ちたら、原因を切り分けて直すのは自分です。

なので私が意識したのは「AIに書かせたコードでも、**壊れたときに自分で直せる状態**にしておく」こと。具体的には以下をルールにしました。

1. **インフラの数字は必ず自分で実測する**（AIの「たぶん大丈夫」を信じない）
2. **本番前に必ず負荷試験を回す**（想定ではなく実測でボトルネックを見る）
3. **AIが書いたコードの「障害時の挙動」を自分で理解しておく**（例外で1回落ちたら全員切れる、という挙動を把握しておく）

この3つが、後述する本番トラブルで効きました。

---

## 設計:130人を1プロセスで捌く

### なぜWebSocketで、なぜ状態は全部メモリか

やりたいことは「全員の画面を、ほぼ同時に、同じ状態にする」。ポーリングだと遅延と負荷が読めないので、サーバー主導でpushできるWebSocketにしました。

状態（出題中の問題、各参加者のピン、Good投票数）は全部サーバー1プロセスのメモリに置いています。正直に言うと、これは最初から緻密に設計したというより、**AIと作り進めるうちに自然とこの形に落ち着いた**というのが実態です。ただ、後から振り返っても、この規模ではこれで正解だったと思っています。

- ステージ企画の2時間ちょっとしか生きないデータで、永続化の要件がない
- 130接続程度なら1プロセスのメモリと1コアで余裕がある（後述の負荷試験で確認）
- プロセス構成が単純なほど、本番で壊れにくい・直しやすい

結果として「スケールする構成」ではなく「**当日確実に動く構成**」になっていた、という感じです。時間がない中では、この割り切りがむしろ良い方向に働きました。

### ブロードキャストの副作用を1回にまとめる

全員に状態を配るとき、参加者ごとに「自分は投票済みか」など個別の値を差し込む必要があります。素直に書くと参加者ごとに集計関数を呼んでしまい、130人ぶん同じ計算が走ります。

そこで、1回のブロードキャストで共有部分は一度だけ計算し、キャッシュして配る形にしました。

```js
function buildGoodStateFor(meta, cache = {}) {
  // buildStatsSummary は maxParticipantCount 更新の副作用を持つため、
  // ブロードキャスト1回につき1度だけ実行する
  if (!cache.goodStats) {
    cache.goodAudience = goodAudienceCount();
    cache.goodStats = buildStatsSummary();
  }
  return {
    mode: "good",
    stats: cache.goodStats,
    audienceCount: cache.goodAudience,
    hasVotedCurrent: hasVotedCurrent(meta), // ここだけ参加者ごと
    // ...
  };
}
```

このコメントに「副作用を持つから1回だけ」と書いてあるのがポイントで、これは後で自分がバグを踏んで理解したことをコードに残したものです。AIが最初に書いた版では、この集計は参加者ごとに毎回呼ばれていました。

### レート制限:トークンバケット

参加者はピンを動かすたびに `answer:update` を送ってきます。ドラッグ中は連射になるので、悪意がなくてもサーバーが詰まりえます。1接続あたりトークンバケット方式で「バースト20通・毎秒10通回復」に制限しました。

```js
// メッセージレート制限（トークンバケット: バースト20通、毎秒10通回復）
```

クライアント側でも300msデバウンスを入れているので、実際は毎秒2通程度。制限の10通は正常操作を邪魔せず、暴走だけ止める値に設定しています。この「正常操作の実測値を先に測って、その外側に制限を引く」順番が大事で、先に制限値を決めると正規ユーザーを弾きます。

### 障害を前提にした設計

サーバーは `uncaughtException` が出たら `process.exit(1)` して落ちる作りになっていました。これはAIが書いた挙動で、正直に言うと最初から意図した設計ではありません。ただコードを読んで「例外が出たら中途半端に生き残らず、いっそ落ちて即再起動したほうが安全」という挙動だと理解したので、**それが安全に成立するように周りを固める**方針にしました。具体的には、

- systemdで自動再起動（`Restart=always`）を必須にする
- ファイルディスクリプタ上限を引き上げる（大量WebSocketで枯渇するため）

をセットにしています。「例外1回で全員切れる」を許容できるのは、再起動が数秒で終わって、クライアントが自動再接続する前提が揃っているからです。この再接続は `clientId` をキーに、同じ人が繋ぎ直したら前の接続を差し替える形で実装されています。

ここが「AIに書かせたコードでも、挙動だけは自分で理解しておく」の実例です。コードを書いた主体がAIでも、その挙動の意味を掴んでいれば、周辺（自動再起動・FD上限）を自分で固めて本番に耐える形にできます。

---

## 本番運用:数字を自分で測る

ここがAIに丸投げできない部分です。「WebSocketは軽いから大丈夫」は本番では通用しないので、全部実測しました。

### 最初は普通に落ちた

先に結論を言うと、**このサーバーは一発では動きませんでした**。本番前、運営メンバー**約60人**で実際に集まってリハーサルを兼ねたテストを3回回したのですが、**1回目・2回目は負荷に耐えられず、全員の接続がまとめて切れてしまいました**。しかもこれは本番想定の130人の半分以下、たった60人での話です。このままでは本番で確実に死ぬ、という状態でした。

原因ははっきりしています。**GEOモードで参加者がピンを打つ（動かす）たびに、全参加者へ全状態をブロードキャストしていた**のです。これは掛け算で効いてきます。参加者が N 人いて、それぞれがピンをドラッグして毎秒何通も `answer:update` を送る。その1通ごとに N 人へ配信するので、送出メッセージ量は **おおよそ N × N のオーダー**で膨らみます。60人でも重なった瞬間に送出帯域がパンクし、WebSocketがまとめて落ちる。おそらく帯域幅の飽和が引き金でした。

```
参加者が1人ピンを動かす
  → その1回で 全員へ全状態をブロードキャスト
60人が同時にドラッグ
  → 60 × 60 ≒ 数千通/秒 が積み上がる（本番130人なら 130 × 130）
  → 送出帯域がパンク → 全員切断
```

ここで効いたのが「AIを道具として握っている」こと。3回目のテストの前に、この症状をAI（Claude Fable 5）に投げてサーバー側を修正してもらい、**3回目でようやく耐えるようになりました**。修正の方向性は「ピン1回ごとに全体ブロードキャストしない」ことに尽きます。具体的には、

- クライアント側でピン送信を**300msデバウンス**して送信頻度そのものを落とす
- サーバー側は**トークンバケットでレート制限**（バースト20通・毎秒10通）して暴走を止める
- ブロードキャスト時の集計を**1回にまとめてキャッシュ**し、配信あたりの計算量を削る

という、送出量とCPUの両方を落とす方向の対策で、上の3つはすべてこのとき入れたものです。そして**3回目のテストは、同じ60人で問題なく通りました**。時間がない中でも、落ちた→原因を切り分ける→AIに直させる→また試す、というループを回せたのが大きかったです。

重要なのは、この反復を**60人リハーサルという「安全に落とせる場」でやり切った**こと。1・2回目で落ちたのがテストで本当に良かった。本番でこれをやったら終わっていました。**「全員がピンを動かし続ける」を最悪ケースとして、この後の負荷試験に組み込んだ**のも、この失敗を踏んだからです。

### 負荷試験(artillery)

60人リハーサルを通した後、さらに機械的に上限を叩くため、最悪ケース = 「GEOモードで150人が出題中にピンを動かし続ける」をartilleryで再現しました。150接続を張り、各接続が毎秒 `answer:update` を送り続けます。

```yaml
config:
  target: "wss://<本番ドメイン>"
  phases:
    - duration: 30
      arrivalRate: 5
      maxVusers: 500
  ws:
    headers:
      # サーバーの Origin 検証(CSWSH対策)を通すため正規Originを送る
      Origin: "https://<クライアントのドメイン>"
scenarios:
  - engine: ws
    flow:
      - send: '{"type":"join","role":"participant","name":"負荷テスト","clientId":"lt_{{ $uuid }}"}'
      - loop:
          - send: '{"type":"answer:update","lat":35.6,"lng":139.7}'
          - think: 1
        count: 60
```

試験中は本番VMで以下を監視しました。

```bash
top -p $(pgrep -f 'node index.js')   # CPUが1コアを食い潰していないか
ss -s | grep estab                    # 実際の確立済み接続数
```

さらに、負荷試験を途中で `Ctrl+C` して数十接続を一斉切断し、**残った接続が巻き込まれて落ちないか（再接続の嵐が起きないか）**も確認しました。これは実際のイベントで「一部の人が電波を失って抜ける」を想定したものです。

### 当日の実測

- **同時接続:130（来場者ほぼ全員がスマホで参加）**
- **nodeプロセスのCPU:1コアの約50%**

1プロセス・DBなしの構成で、想定の150に対して実測130、CPUは半分。事前の負荷試験どおりに収まりました。「たぶん大丈夫」ではなく「150まで試して大丈夫だと確認した」状態で本番に臨めたのが、精神的にいちばん大きかったです。

---

## 本番トラブル:Good率が全員100%に張り付いた

無事に完走はしました。が、GOT TALENTモードで**投票中に全パフォーマーのGood率が100%付近に張り付く**トラブルが起きました。

### 原因

Good率は `Good投票数 ÷ 参加人数` です。問題は分母の「参加人数」を、**その瞬間の接続者数（ライブの人数）**で計算していたこと。

投票が進むと、投票し終わって画面を閉じる・電波が切れて抜ける人が出ます。すると分母だけが減り、分子（投票数）は残るので、率がぐんぐん上がって100%に張り付いてしまう。全員が高評価に見える、実質使い物にならない状態です。

### その場の対応

原因の完全特定より、**まず企画を止めないこと**を優先しました。私はステージ統括だったので、その場で「全出演者の出演が終わったあとに再投票する」と判断し、司会にそう伝達。同時に**照明・音響・スクリーンの各担当にも情報を共有**して、再投票の段取りを全員で揃えました。観客には隠さず「システムトラブルです」と正直に伝えたうえで、あらためて投票し直してもらい、企画自体は最後まで成立させました。

そしてここが本当に大変だったところなのですが、**私自身が最後の出演者（けん玉）としてステージに立っていました**。つまりトラブルが起きている最中、自分は演技中で、スタッフに指示を出すことも状況を共有することもできない時間帯があったのです。統括と出演者を兼ねていたせいで、いちばん動きたい瞬間に動けない。この「人が足りない・自分が分身できない」状況こそ、一人で運営とアプリを抱えた代償でした。

判断としては「システムで完璧に直す」より「運用で丸める」が正解だったと思っています。本番中にコードを直すのが一番危ない。**壊れても運用でリカバリできる余地を、司会・照明・音響と共有しておいたことに救われました**。

### 恒久対応

本番後、**AIに依頼して**分母を**「投票期間中の最大接続者数（high-water mark）」**に変えてもらいました。一度カウントした人数は、その後抜けても減らさない。さらに投票者数を下回らないことも保証します。

```js
// 分母は投票期間中の最大接続者数（high-water mark）。
// ライブ接続数をそのまま使うと、投票後に退出した人のぶん分母が減って
// Good率が100%を超えてしまう。投票者数を下回らないようにも保証する。
const participantCount = isCurrent && !stat.locked
  ? Math.max(stat.maxParticipantCount, goodCount)
  : Math.max(stat.participantCount, stat.goodCount);
```

コード上のコメントが、そのまま反省文になっています。

### この一件で学んだこと

リアルタイム系のバグは、**「人が減る」という一番地味な入力**で壊れます。厄介なのは、これは「考えていなかった」わけではないことです。実際、投票中に人が抜ける可能性は認識していました。それでも、**認識していたのに対策が詰め切れていなかった**——分母をライブ接続数のままにしていた——ので、本番で刺さりました。

「気づいていなかった」より「気づいていたのに対策が甘かった」ほうが、正直こたえます。リスクを頭で分かっているだけでは足りず、**それを実際に潰したかどうか（分母を固定したか）を、テストで確かめるところまでやらないと意味がない**。これが今回いちばん自分に残った教訓です。負荷試験を「増える方向」だけでなく「投票途中で人が抜ける方向」まで実際に流していれば、本番前に踏めたはずでした。

---

## AIとの分担、実際どうだったか

最後に、いちばん書きたかったところを。

**AIが得意で、任せて良かったところ**

- WebSocketのメッセージ設計やブロードキャストの雛形
- systemd / nginx / sysctl の設定ファイルの叩き台
- artillery設定やデプロイ手順など「定型だが調べるのが面倒」な部分
- 落ちた原因（ピン毎ブロードキャスト）を投げたら、デバウンス＋レート制限＋キャッシュで**具体的に直してくれた**

**AIに任せず、自分でやらないと死んだところ**

- **数字の実測**（CPU・接続数）。AIは実測できない。60人で落ちることも、130人で50%に収まることも、人が回さないと分からない
- **AIが書いたコードの「障害時の挙動」を理解しておくこと**（落ちたら即再起動、を前提に周りを固める）
- **本番トラブル時の判断**（コードを直すか、運用で丸めるか。しかも自分は出演中だった）
- **「気づいていたリスクを、テストで実際に潰し切ること」**（減る方向は認識していたのに詰め切れなかった）

結論として、AIは「動くものを速く作る」には最高でしたが、**本番で落とさない／落ちても直せる状態を作るのは、最後まで人間の仕事**でした。逆に言えば、そこさえ自分で握っていれば、AIを全力で使って個人開発を本番運用まで持っていけます。

そして今回のように、**開発以外（学祭運営）に時間を取られている状況ほど、この分担は効きます**。定型作業をAIに寄せて、自分は「実測」と「本番判断」という人間にしかできないところに時間を集中する。時間がない中で本番の質を落とさないための、現実的な戦い方だと思っています。

130人が同時に自分のアプリを触っていて、しかもトラブルからちゃんと立て直せた。この経験は、AIをどれだけ使ったかとは関係なく、確かに自分の実力として残りました。

---

## まとめ

- リアルタイム対戦アプリ（WebSocket + Node 1プロセス + メモリ状態）を個人開発し、学祭で**130同時接続・CPU 1コア50%**で完走
- **60人リハーサルの1・2回目でサーバーが落ちた**（ピン操作ごとの全体ブロードキャストで帯域がパンク＝実質O(N²)）。3回目までにAI（Claude Fable 5）でデバウンス＋レート制限＋集計キャッシュを入れて修正、3回目は無事通過。さらにartilleryで150まで実測して本番に臨んだ
- **Good率が全員100%に張り付くバグ**を、ステージ統括として司会・照明・音響と連携し再投票運用で丸め（しかも自分は最後の出演者だった）、恒久対応でhigh-water mark方式に修正
- AIは実装を速くしてくれるが、**実測・本番判断・仕様の穴は人間が握る**。そこを握ればAI併用で本番まで行ける

同じように「AIで作ったものを本番に出すのが不安」な人の、背中を押せたら嬉しいです。
